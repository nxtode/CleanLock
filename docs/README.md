# CleanLock Appcast

This directory is published by GitHub Pages and hosts the Sparkle appcast:

```text
https://nxtode.github.io/CleanLock/appcast.xml
```

Generate the appcast after packaging release assets:

```sh
./script/package_release.sh
./script/sparkle_generate_appcast.sh
```
